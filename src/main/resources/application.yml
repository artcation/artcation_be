spring:
  application:
    name: artcation

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

logging:
  pattern:
    level: "%5p [%X{requestId:-}] %X{method:-} %X{uri:-}"